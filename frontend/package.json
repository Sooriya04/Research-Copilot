{
  "name": "research-copilot-frontend",
  "version": "2.0.0",
  "private": true,
  "description": "Precision Scientific Workstation React Frontend for Research Copilot",
  "main": "src/index.jsx",
  "scripts": {
    "build": "esbuild src/index.jsx --bundle --outfile=../public/bundle.js --loader:.jsx=jsx --loader:.js=jsx --minify"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
